enum ApiState { empty, loading, failure, success }

enum Operation { select, create, update, delete }
enum CartOperation { add, remove, delete,clearAll }
enum AddRasiDropDown { rasi, natchathra }
enum SignUpField { name, userName, email,password }






